# ============================================
# Environment Configuration Template
# ============================================
# Copy this file to .env.development.local for local development
# Copy this file to .env.production.local for local production testing
# Never commit .env*.local files to git (they're in .gitignore)

# ============================================
# Environment
# ============================================
# Set to 'development' or 'production'
# This determines which environment the app runs in
NEXT_PUBLIC_ENV=development

# Base URL for the application
# Development: http://localhost:3000
# Production: https://jewelsbynavkush.com
# Dev Environment: https://dev2026.jewelsbynavkush.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Site name (optional, defaults to 'Jewels by NavKush')
NEXT_PUBLIC_SITE_NAME=Jewels by NavKush

# ============================================
# MongoDB Atlas Configuration
# ============================================
# Get connection string from MongoDB Atlas → Clusters → Connect → Connect your application
# Format: mongodb+srv://<username>:<password>@jewelsbynavkush.rbypdri.mongodb.net/<database>?retryWrites=true&w=majority&appName=jewelsbynavkush
# 
# Development: Use 'dev' database with 'jewelsbynavkush-user-dev' user
# Production: Use 'prod' database with 'jewelsbynavkush-user-prod' user

MONGODB_URI=

JWT_SECRET=
JWT_EXPIRES_IN=5m
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
